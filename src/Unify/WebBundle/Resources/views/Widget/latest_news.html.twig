<!-- aside -->
   <div class="aside">
      <div class="inside">
         <h2>Latest News</h2>
         <ul class="news">
            {% for n in news %}
            <li>
                <a href="{{ path('news', {'slug': n.slug }) }}">{{ n.getCreatedAt.format('M d, Y') }}</a>
                <strong>{{ n.title }}</strong>
                {{ n.content|summary}}
            </li>
            {% endfor %}
         </ul>
      </div>
   </div>